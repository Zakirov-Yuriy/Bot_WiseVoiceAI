![Bot Logo](images/Frame.png)

# Telegram-бот "Transcribe To!" (WiseVoiceAI)

Этот Telegram-бот предназначен для преобразования аудио- и видеосообщений в текстовый формат. Он поддерживает загрузку файлов, ссылки на YouTube и предоставляет несколько форматов для вывода текста.

## 🚀 Основные возможности

- **Транскрибация аудио**: Преобразование аудиофайлов (MP3, WAV, OGG и др.) в текст.
- **Транскрибация видео с YouTube**: Отправьте боту ссылку на видео, и он извлечет аудиодорожку для транскрибации.
- **Распознавание спикеров**: Возможность выделить реплики разных людей в диалоге.
- **Тайм-коды**: Добавление временных меток к расшифрованному тексту.
- **Разные форматы вывода**:
  - `Google Docs`
  - `Microsoft Word (.docx)`
  - `PDF`
  - `Обычный текст (.txt)`
  - `Markdown (.md)`
- **Система подписки**: Интеграция с платежной системой **ЮMoney** для платного доступа к расширенным функциям.
- **Автоматическая активация подписки**: Webhook сервер для мгновенной активации подписки после оплаты.
- **Информация о пользователе**: Кнопка "👤 Пользователь" для просмотра статуса подписки и оставшихся попыток.
- **Пробный период**: Новые пользователи получают 3 бесплатные попытки транскрибации.
- **Локализация**: Поддержка русского и английского языков.

## 🛡️ Надежность и производительность

### Кеширование (Redis)
- **Результаты транскрибации**: Автоматическое кеширование результатов обработки аудио файлов
- **Пользовательские данные**: Кеширование информации о пользователях для быстрого доступа
- **TTL (Time To Live)**: Настраиваемое время жизни кеша для оптимизации памяти

### Очереди задач (Celery + Redis)
- **Асинхронная обработка**: Тяжелые задачи транскрибации выполняются в фоне
- **Масштабируемость**: Поддержка нескольких воркеров для параллельной обработки
- **Мониторинг задач**: Отслеживание статуса выполнения задач в реальном времени

### Rate Limiting
- **Защита от спама**: Ограничение количества запросов от пользователей
- **Многоуровневая защита**: Burst limit (короткие всплески) + hourly/daily limits
- **Автоматическое восстановление**: Градуальное снятие ограничений

### Мониторинг (Prometheus + Grafana)
- **Метрики производительности**: Время обработки запросов, кэш-хиты, ошибки
- **Системные метрики**: Использование CPU, памяти, Redis
- **Визуализация**: Готовые дашборды для мониторинга состояния бота
- **Алёртинг**: Настраиваемые уведомления о проблемах

### Error Tracking (Sentry)
- **Отслеживание ошибок**: Автоматический сбор и анализ исключений
- **User Context**: Информация о пользователях при ошибках
- **Performance Monitoring**: Трассировка производительности ключевых операций

## 🛠️ Технологии

- **Python 3.11+**
- **aiogram 3**: Асинхронный фреймворк для создания Telegram-ботов.
- **FastAPI**: Веб-фреймворк для webhook сервера.
- **YooMoney API**: Для обработки платежей.
- **AssemblyAI API**: Сервис распознавания речи.
- **OpenRouter API**: Альтернативный API для ИИ моделей.

## 💳 Система платежей и подписок

### Автоматическая активация подписки

Бот использует **webhook сервер** для автоматической обработки платежей от YooMoney:

1. **Пользователь нажимает "Оформить подписку"**
2. **Создается платежная ссылка YooMoney**
3. **Пользователь оплачивает подписку**
4. **YooMoney отправляет уведомление на webhook URL**
5. **Webhook сервер автоматически активирует подписку**

### Webhook сервер

Webhook сервер (`webhook_server.py`) работает на порту 8001 и предоставляет:

- **POST /yoomoney/webhook**: Обработка платежных уведомлений
- **GET /health**: Проверка работоспособности сервера

### Ручная активация (резервный вариант)

Если webhook недоступен, администратор может активировать подписку вручную:

```bash
# Активация подписки по лейблу платежа
/confirm_payment sub_123456789_abc123...
```

### Настройка webhook в YooMoney

1. **Войдите в ваш YooMoney аккаунт**: https://yoomoney.ru/
2. **Перейдите в раздел "Инструменты для разработчиков"**
3. **Добавьте HTTP-уведомление**:
   - **URL для уведомлений**: `https://your-domain.com/yoomoney/webhook`
   - **Метод**: POST
   - **Формат**: application/x-www-form-urlencoded

### Переменные окружения для платежей

```env
ENABLE_PAYMENTS=true
YOOMONEY_WALLET=4100111325360739
YOOMONEY_CLIENT_ID=your_client_id
YOOMONEY_CLIENT_SECRET=your_client_secret
YOOMONEY_WEBHOOK_URL=https://your-domain.com/yoomoney/webhook
PAYMENT_AMOUNT=20
SUBSCRIPTION_DURATION_DAYS=30
```

### Запуск webhook сервера

```bash
# На сервере с публичным IP
python webhook_server.py

# С HTTPS (рекомендуется для продакшена)
uvicorn webhook_server:app --host 0.0.0.0 --port 8001 --ssl-keyfile /path/to/key.pem --ssl-certfile /path/to/cert.pem
```

### Тестирование webhook

```bash
# Протестировать endpoint
curl -X POST https://your-domain.com/test/webhook \
  -d "test=data"
```

## 🏗️ DevOps и развертывание

### CI/CD Pipeline

Проект использует GitHub Actions для автоматического тестирования и развертывания:

- **Автоматическое тестирование**: Запуск тестов при каждом push и pull request
- **Линтинг и форматирование**: Black, isort, flake8, mypy
- **Покрытие кода**: Отчеты о покрытии с загрузкой в Codecov
- **Docker сборка**: Автоматическая сборка и публикация Docker образов
- **Качественные gates**: Сборка проходит только при успешном прохождении всех проверок

### Качество кода

- **Black**: Автоматическое форматирование кода
- **isort**: Сортировка импортов
- **flake8**: Линтинг и проверка стиля
- **mypy**: Статическая типизация
- **pre-commit hooks**: Автоматическая проверка перед коммитом

### Структурированное логирование

Логи выводятся в JSON формате для совместимости с ELK стеком:

```json
{
  "@timestamp": "2025-11-02T14:11:37.000Z",
  "level": "INFO",
  "logger": "wisevoiceai-bot",
  "message": "Бот запущен с поддержкой кеширования, очередей, rate limiting и мониторинга",
  "module": "bot",
  "function": "main",
  "line": 58,
  "process": 1234,
  "thread": 5678,
  "thread_name": "MainThread",
  "component": "wisevoiceai-bot"
}
```

**Особенности логирования:**
- Структурированный JSON формат
- Контекст пользователя в логах
- Информация об исключениях
- Совместимость с Elasticsearch, Logstash, Kibana (ELK)

## ⚙️ Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Zakirov-Yuriy/Bot_WiseVoiceAI.git
   cd Bot_WiseVoiceAI
   ```

2. **Создайте и активируйте виртуальное окружение:**
   ```bash
   python -m venv venv
   venv\Scripts\activate  # Для Windows:
   source venv/bin/activate   # Для Mac:
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройте переменные окружения:**
   Создайте файл `.env` в корне проекта и добавьте в него следующие переменные:
   ```env
   TELEGRAM_BOT_TOKEN="ВАШ_ТОКЕН_TELEGRAM"
   YOOMONEY_WALLET="ВАШ_КОШЕЛЕК_YOOMONEY"
   YOOMONEY_SECRET_KEY="ВАШ_СЕКРЕТНЫЙ_КЛЮЧ_YOOMONEY"
   # Другие необходимые переменные...
   ```

5. **Запустите бота:**
   ```bash
   python bot.py
   ```

6. **(Опционально) Запустите webhook сервер для автоматической активации подписок:**
   ```bash
   python webhook_server.py
   ```

   Webhook сервер необходим для автоматической обработки платежей от YooMoney. Без него подписки придется активировать вручную через админские команды.

## 🗄️ Работа с базами данных

Проект использует две базы данных:
- **Удаленная MySQL** (Railway) - основная база данных для продакшена
- **Локальная SQLite** - для разработки и резервного копирования

### 📊 Просмотр данных

#### Просмотр всех баз данных (локальная + удаленная):
```bash
python view_database_data.py
```

#### Просмотр только локальной базы данных:
```bash
python view_local_database.py
```

### 📥 Скачивание данных из MySQL в SQLite

Для создания резервной копии или локального тестирования:
```bash
python download_mysql_to_local.py
```

Этот скрипт:
- Подключается к удаленной MySQL базе данных
- Скачивает все данные пользователей
- Сохраняет их в локальную базу данных `users.db`

### 🏗️ Структура данных пользователей

| Поле | Описание |
|------|----------|
| `user_id` | Telegram ID пользователя |
| `trials_used` | Количество использованных бесплатных попыток |
| `is_paid` | Статус подписки (true/false) |
| `subscription_expiry` | Timestamp окончания подписки |
| `referrer_id` | ID пользователя, который пригласил этого пользователя |
| `referral_code` | Уникальный реферальный код |
| `free_weeks` | Количество бесплатных недель от рефералов |

### 🔧 Миграция данных

Для переноса данных из SQLite в MySQL используйте:
```bash
python migrate_to_mysql.py
```

### ➕ Добавление пользователей

Для добавления нового пользователя в локальную базу данных:
```bash
python add_user_to_local_db.py
```

Или создайте свой скрипт с использованием функции `add_user_to_local_db()` из модуля.

### 📤 Загрузка пользователей в MySQL

Для загрузки пользователя из локальной SQLite базы в удаленную MySQL базу данных:
```bash
python upload_user_to_mysql.py
```

Этот скрипт:
- Получает данные пользователя из локальной базы `users.db`
- Загружает их в удаленную MySQL базу данных
- Обновляет существующие записи или создает новые

### 🧪 Тестирование подключения

Проверка подключения к MySQL базе данных:
```bash
python test_mysql_connection.py
```

## 🚀 Развертывание с Docker

### Быстрый запуск с docker-compose

1. **Настройте переменные окружения:**
   Создайте файл `.env` с необходимыми переменными (см. пример выше)

2. **Запустите все сервисы:**
   ```bash
   docker-compose up -d
   ```

3. **Проверьте статус:**
   ```bash
   docker-compose ps
   ```

### Доступ к сервисам

- **Bot**: Работает внутри контейнера
- **Prometheus**: http://localhost:9090
- **Grafana**: http://localhost:3000 (admin/admin)
- **Redis**: localhost:6379

### Масштабирование

```bash
# Добавить больше Celery воркеров
docker-compose up -d --scale celery_worker=3

# Масштабировать Redis (если необходимо)
docker-compose up -d --scale redis=2
```

## 📊 Мониторинг

### Метрики Prometheus

Бот экспортирует метрики на порту 8000 по пути `/metrics`:
- `bot_requests_total` - Общее количество запросов
- `bot_transcription_duration_seconds` - Время транскрибации
- `bot_cache_hits_total` - Попадания в кэш
- `bot_errors_total` - Количество ошибок

### Grafana Dashboard

Готовый дашборд включает:
- **Request Rate**: График частоты запросов
- **Transcription Performance**: Производительность транскрибации
- **Cache Performance**: Эффективность кэширования
- **Rate Limiting**: Нарушения лимитов
- **Error Rate**: Уровень ошибок
- **Active Users**: Активные пользователи

### Sentry Error Tracking

Для настройки Sentry:
1. Создайте проект на https://sentry.io
2. Добавьте `SENTRY_DSN` в переменные окружения
3. Ошибки будут автоматически отслеживаться и группироваться

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Значение по умолчанию |
|------------|----------|----------------------|
| `REDIS_URL` | URL Redis сервера | `redis://localhost:6379/0` |
| `REDIS_CACHE_TTL` | TTL для кэша результатов (сек) | `3600` |
| `REDIS_USER_CACHE_TTL` | TTL для кэша пользователей (сек) | `1800` |
| `CELERY_BROKER_URL` | URL брокера Celery | `redis://localhost:6379/1` |
| `CELERY_RESULT_BACKEND` | Backend для результатов | `redis://localhost:6379/2` |
| `SENTRY_DSN` | DSN для Sentry | - |
| `PROMETHEUS_PORT` | Порт для метрик | `8000` |

### Rate Limiting

Настраивается в коде (`middleware.py`):
- **Burst limit**: 10 запросов за 10 секунд
- **Minute limit**: 30 запросов в минуту
- **Hour limit**: 100 запросов в час

### Кэширование

- **Результаты транскрибации**: Кэшируются на 1 час
- **Пользовательские данные**: Кэшируются на 30 минут
- **Автоматическая инвалидация**: При изменении данных пользователя
